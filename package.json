{
	"name": "frog-tcg-final",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build:css": "tailwindcss -i ./tailwind-input.css -o ./public/tailwind-built.css --minify",
		"build:js": "esbuild --bundle=false --minify --allow-overwrite public/app.js --outfile=public/app.js && esbuild --bundle=false --minify --allow-overwrite public/dashboard.js --outfile=public/dashboard.js && esbuild --bundle=false --minify --allow-overwrite public/card-creator.js --outfile=public/card-creator.js",
		"deploy": "npm run build:js && wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@playwright/test": "^1.59.1",
		"esbuild": "^0.25.12",
		"tailwindcss": "^3.4.17",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.54.0"
	},
	"dependencies": {
		"@resvg/resvg-wasm": "^2.6.2",
		"@supabase/supabase-js": "^2.89.0",
		"@upstash/redis": "^1.37.0",
		"jose": "^6.1.3",
		"stripe": "^21.0.1"
	}
}
